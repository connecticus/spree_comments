There are the comments left during the day: <%= @date.strftime %> on <%= @tenant %> shop:

<ul>
  <% @comments.each do |comment| %>
    <li>
      <p>
      From <%= comment.user.email %> to <%= comment.commentable_type.constantize.name %> number <%= comment.commentable.number %>:
      </p>
      <br />
      <hr />
      <p><%= comment.comment %></p>
    </li>
  <% end %>
</ul>
